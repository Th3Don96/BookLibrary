package com.pluralsilght;

import java.util.Scanner;


public class LibraryHomeScreen extends Books {
    public static void main(String[] args) {
        Scanner keyboard = new Scanner(System.in);

        //User Input Search
        System.out.print("Search Book:");
        String books = keyboard.nextLine();
        if (String checkedIn = not availbe){
            (System.out.println(bookTitle,idNumber+" is "+checkedIn+"Checked out to: ",checkedOut));
            else (System.out.println(bookTitle,idNumber+" is "+checkedIn,checkedOut));
    }


}